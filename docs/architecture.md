# Milestone 1 architecture

```text
Next.js dashboard → FastAPI → LangGraph (extract → verify → draft)
```

The deterministic local extractor is the zero-cost default. No patient data is persisted in this milestone, and approval is intentionally only a visual demo.
